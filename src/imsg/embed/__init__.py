"""S6 — embed + index (SPEC §8 S6, §7.3): the primary/multimodal
embedding providers (`provider.py`), the Postgres embedding pipeline
(`pipeline.py`), and the SQLite FTS5 sidecar (`fts/`).
"""

from __future__ import annotations
